<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/transtrack_primary</item>
        <item name="colorPrimaryDark">@color/transtrack_primary_dark</item>
        <item name="colorAccent">@color/transtrack_primary</item>
        <item name="android:colorPrimary">@color/transtrack_primary</item>
        <item name="android:colorPrimaryDark">@color/transtrack_primary_dark</item>
        <item name="android:colorAccent">@color/transtrack_primary</item>
        <item name="android:statusBarColor">@color/transtrack_primary_dark</item>
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

</resources>
